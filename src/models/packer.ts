export type PackerDTO = {
  id: string;
  name: string;
  packsCount: number;
  perValue: number;
};

export type Packer = {
  id: string;
  name: string;
  packsCount: number;
  perValue: number;
};
